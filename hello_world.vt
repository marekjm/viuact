; This is a oneline comment.

(let log (x) {
    (print x)
    0
})

(let logl (~x) {
    (print x)
    0
})

(let main () {
    (log "Hello, World!")

    (let x "What's up?")
    (print x)
    (logl (~x x))

    (log "Good bye, World!")
    0
})
