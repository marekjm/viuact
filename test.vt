(enum example (
    Foo
    (Bar 'a)
))

(val main () i8)
(let main () {
    ; operator :: is for path resolution
    ; operator . is for struct field access
    ; (let x (Some::Example::example::Foo 42))
    ;(let x (example::Foo 42))
    ;(let x example::Foo)
    (let x (example::Foo))
    (print x)

    (let y (example::Bar 42))
    (print y)

    (match x (
        (with Foo (print "nothing"))
        (with Bar x' (print x'))))

    (print (match y (
        (with Foo -1)
        ; (with Bar x' (print x')))))
        (with Bar x' x' ))))

    0
})
